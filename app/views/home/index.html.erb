<script>
  $(document).ready(function(){
    <% @articles.each do |article|  %>
    var map = new google.maps.Map(document.getElementById('map_<%= article.id %>'), {
      center: {lat: <%= article.lat %>, lng: <%= article.lng %>},
      zoom: 4
    });
    var marker = new google.maps.Marker({
      map: map,
      position: new google.maps.LatLng(<%= article.lat %>,<%= article.lng %>),
      title: 'Hello World!'
    });
    <% end %>

  });
</script>

<!-- Carousel
================================================== -->
<div id="myCarouselHome" class="carousel slide" data-ride="carousel">
  <!-- Indicators -->
  <ol class="carousel-indicators">
    <li data-target="#myCarouselHome" data-slide-to="0" class="active"></li>
    <li data-target="#myCarouselHome" data-slide-to="1"></li>
    <li data-target="#myCarouselHome" data-slide-to="2"></li>
  </ol>
  <div class="carousel-inner" role="listbox">
    <div class="item active">
      <img class="first-slide" src="assets/kinkakuji.jpg" alt="First slide">
      <div class="container">
        <div class="carousel-caption">
          <h1>Kinkaku-ji</h1>

        </div>
      </div>
    </div>
    <div class="item">
      <img class="second-slide" src="assets/kiyomizu.jpg" alt="Second slide">
      <div class="container">
        <div class="carousel-caption">
          <h1>Kiyomizu Temple</h1>

        </div>
      </div>
    </div>
    <div class="item">
      <img class="third-slide" src="assets/fushimi.jpg" alt="Third slide">
      <div class="container">
        <div class="carousel-caption">
          <h1>Fushimi Shrine</h1>
        </div>
      </div>
    </div>
  </div>

</div><!-- /.carousel -->


<!-- Marketing messaging and featurettes
================================================== -->
<!-- Wrap the rest of the page in another container to center all the content. -->

<div class="container marketing">
  <h1>
    Popular Articles
  </h1>
  <hr style="border-top: 1px solid #aaaaaa"/>
  <!-- Three columns of text below the carousel -->
  <div>
  <% @articles.each do |article|  %>
      <div style="height: 200px;">
        <div class="article">
            <h2 style="margin-top: 5px"><%= link_to article.title, article %></h2>
            <p id="content"><%= sanitize(article.content.truncate(400))%></p>
        </div>
        <div id="map_<%= article.id%>" class="map" style="height: 100%;">
          nothing
        </div>
      </div>
        <hr style="border-top: 1px solid #aaaaaa"/>
  <% end %>
  </div>


  <!-- /END THE FEATURETTES -->


  <!-- FOOTER -->


</div><!-- /.container -->


<!-- Bootstrap core JavaScript
================================================== -->
<!-- Placed at the end of the document so the pages load faster -->

